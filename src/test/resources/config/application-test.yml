wiremock:
  server:
    port: 4567

external:
  customer-report-calculation:
    url: http://localhost:${wiremock.server.port}

#Open-feign client
feign:
  client:
    config:
      default:
        connectTimeout: 10000
        readTimeout: 20000
        loggerLevel: full